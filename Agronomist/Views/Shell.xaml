<Page x:Class="TheDream.Views.Shell"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:TheDream.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <!-- Page Resources -->
    <Page.Resources>
    </Page.Resources>

    <SplitView x:Name="navigation"
               Style="{ThemeResource NavigationPanel}"
               IsPaneOpen="True">

        <!-- Navigation Menu -->
        <SplitView.Pane>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="112" />
                    <RowDefinition />
                    <RowDefinition Height="144" />
                </Grid.RowDefinitions>

                <!-- Pane Header -->
                <Grid BorderBrush="#66EBEBEB"
                      BorderThickness="0,0,0,1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="96" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <Button x:Name="rocketButton"
                            Style="{ThemeResource NoBackgroundDarkTheme}"
                            BorderBrush="{x:Null}"
                            Click="rocketButtonClicked">
                        <Image Height="44"
                               Margin="32,32,0,32"
                               HorizontalAlignment="Left"
                               Stretch="Uniform"
                               Opacity="0.95"
                               Source="ms-appx:///Assets/logo-rocket-light.png" />
                    </Button>
                    <TextBlock Grid.Column="1"
                               FontFamily="Segoe UI"
                               FontWeight="Normal"
                               Foreground="#FFEBEBEB"
                               FontSize="24"
                               Text="Quickbird"
                               Margin="0,40" />
                </Grid>

                <!-- Pane Body -->
                <ListView x:Name="menu"
                          Grid.Row="1"
                          RequestedTheme="Dark"
                          ItemContainerStyle="{ThemeResource MenuItemDark}"
                          SelectionChanged="CropSelected">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="96" />
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="44" />
                                </Grid.ColumnDefinitions>
                                <Ellipse Fill="#FFEBEBEB"
                                         Margin="24,12,24,12" />
                                <Ellipse Fill="Gold"
                                         Margin="24,12,24,12"
                                         Visibility="{Binding IsVisible}" />
                                <TextBlock Text="{Binding IconLetter}"
                                           Foreground="#FF515C6B"
                                           Width="20"
                                           Height="40"
                                           FontSize="30"
                                           TextAlignment="Center"
                                           Margin="38,14,38,17" />
                                <StackPanel Grid.Column="1"
                                            Margin="0,12,0,12">
                                    <TextBlock FontFamily="Segoe UI"
                                               FontWeight="Normal"
                                               FontSize="18"
                                               Text="{Binding CropName}" />
                                    <TextBlock Style="{ThemeResource Subtitle}"
                                               Text="{Binding BoxName}" />
                                </StackPanel>
                                <TextBlock Grid.Column="2"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Right"
                                           Margin="0,0,12,4"
                                           FontFamily="Segoe UI Symbol"
                                           FontSize="28"
                                           Foreground="Gold"
                                           Text="⚠"
                                           Visibility="{Binding IsVisible}">
                                </TextBlock>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Pane Footer -->
                <StackPanel Grid.Row="2">

                    <!-- Add Crop Button -->
                    <Button x:Name="addCropButton"
                            Style="{ThemeResource NoBackgroundDarkTheme}"
                            BorderThickness="0,1,0,1"
                            BorderBrush="#66EBEBEB">
                        <Grid Height="72">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="96" />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <TextBlock Style="{ThemeResource Icon}"
                                       Foreground="#FFEBEBEB"
                                       FontSize="28"
                                       Margin="24,0,24,0"
                                       Text="" />
                            <TextBlock Grid.Column="1"
                                       FontFamily="Segoe UI"
                                       FontWeight="Normal"
                                       FontSize="20"
                                       VerticalAlignment="Center"
                                       Text="Add Crop" />
                        </Grid>
                    </Button>

                    <!-- Settings Button -->
                    <Button x:Name="settingsButton"
                            Style="{ThemeResource NoBackgroundDarkTheme}"
                            BorderThickness="0">
                        <Grid Height="72">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="96" />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <TextBlock Style="{ThemeResource Icon}"
                                       Foreground="#FFEBEBEB"
                                       FontSize="28"
                                       Margin="24,0,24,0"
                                       Text="" />
                            <TextBlock Grid.Column="1"
                                       FontFamily="Segoe UI"
                                       FontWeight="Normal"
                                       FontSize="20"
                                       VerticalAlignment="Center"
                                       Text="Settings" />
                        </Grid>
                    </Button>
                </StackPanel>
            </Grid>
        </SplitView.Pane>

        <SplitView.Content>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="112" />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="330" />
                </Grid.ColumnDefinitions>

                <!-- Page Header -->
                <Grid x:Name="pageHeader"
                      Grid.ColumnSpan="2"
                      Style="{ThemeResource PageHeaderLightTheme}"
                      VerticalAlignment="Stretch"
                      d:LayoutOverrides="LeftMargin, RightMargin, TopMargin, BottomMargin">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="330" />
                    </Grid.ColumnDefinitions>

                    <!-- Crop Run Info -->
                    <RelativePanel Margin="0"
                                   HorizontalAlignment="Stretch"
                                   VerticalAlignment="Stretch">
                        <ScrollViewer Margin="0,14,0,14"
                                      VerticalScrollMode="Disabled"
                                      VerticalScrollBarVisibility="Hidden"
                                      HorizontalScrollMode="Enabled"
                                      HorizontalScrollBarVisibility="Auto"
                                      RelativePanel.AlignLeftWithPanel="True"
                                      RelativePanel.AlignRightWithPanel="True"
                                      BorderThickness="0,0,1,0"
                                      BorderBrush="#4C515C6B">
                            <ScrollViewer.Content>
                                <StackPanel x:Name="cropInfoPanel"
                                            Orientation="Horizontal"
                                            Padding="0,14,0,14">
                                    <StackPanel x:Name="cropNameCard"
                                                Margin="48,0,72,0">
                                        <TextBlock FontSize="24"
                                                   Text="Strawberry"
                                                   TextTrimming="CharacterEllipsis"
                                                   Foreground="#FF515C6B" />
                                        <TextBlock Style="{ThemeResource Subtitle}"
                                                   Text="Crop"
                                                   Foreground="#FF515C6B" />

                                    </StackPanel>
                                    <StackPanel x:Name="varietyNameCard"
                                                Margin="0,0,72,0">
                                        <TextBlock FontSize="24"
                                                   Text="Sweet Juicy Type"
                                                   Foreground="#FF515C6B"
                                                   TextTrimming="CharacterEllipsis" />
                                        <TextBlock Style="{ThemeResource Subtitle}"
                                                   Text="Variety"
                                                   Foreground="#FF515C6B" />

                                    </StackPanel>
                                    <StackPanel x:Name="plantDateCard"
                                                Margin="0,0,72,0">
                                        <TextBlock FontSize="24"
                                                   Text="May 19"
                                                   Foreground="#FF515C6B"
                                                   TextTrimming="CharacterEllipsis" />
                                        <TextBlock Style="{ThemeResource Subtitle}"
                                                   Text="Planted"
                                                   Foreground="#FF515C6B" />

                                    </StackPanel>
                                    <StackPanel x:Name="yieldCard"
                                                Margin="0,0,72,0">
                                        <TextBlock FontSize="24"
                                                   Text="724 kg"
                                                   Foreground="#FF515C6B" />
                                        <TextBlock Style="{ThemeResource Subtitle}"
                                                   Text="Yield So Far"
                                                   Foreground="#FF515C6B" />
                                    </StackPanel>
                                </StackPanel>
                            </ScrollViewer.Content>
                        </ScrollViewer>
                    </RelativePanel>

                    <StackPanel x:Name="ButtonsPanel"
                                Orientation="Horizontal"
                                Grid.Column="1">
                        <Button x:Name="recordYieldButton"
                                Style="{ThemeResource NoBackgroundDarkTheme}"
                                BorderBrush="{x:Null}"
                                Width="112"
                                Padding="18,12,12,12"
                                RequestedTheme="Light">
                            <StackPanel VerticalAlignment="Bottom"
                                        HorizontalAlignment="Stretch">
                                <TextBlock Style="{ThemeResource Icon}"
                                           Margin="0,0,0,12">
                                	<Span FontSize="22"
                                          FontFamily="Segoe MDL2 Assets">
                                		<Run Text="&#xE109;" />
                                	</Span>
                                </TextBlock>
                                <TextBlock FontSize="16"
                                           Foreground="#FF4A90E2"
                                           Text="Record Yield"
                                           TextAlignment="Center"
                                           LineHeight="20"
                                           TextWrapping="WrapWholeWords" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="syncButton"
                                Style="{ThemeResource NoBackgroundDarkTheme}"
                                BorderBrush="{x:Null}"
                                Width="104"
                                Padding="12"
                                RequestedTheme="Light">
                            <StackPanel VerticalAlignment="Bottom"
                                        HorizontalAlignment="Stretch">
                                <TextBlock Style="{ThemeResource Icon}"
                                           Text=""
                                           Margin="0,0,0,12" />
                                <TextBlock FontSize="16"
                                           Foreground="#FF4A90E2"
                                           Text="Refresh Now"
                                           TextAlignment="Center"
                                           LineHeight="20"
                                           TextWrapping="WrapWholeWords" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="showAlertsButton"
                                Style="{ThemeResource NoBackgroundDarkTheme}"
                                BorderBrush="{x:Null}"
                                Width="104"
                                Padding="12"
                                Click="showAlerts"
                                RequestedTheme="Light">
                            <StackPanel VerticalAlignment="Bottom"
                                        HorizontalAlignment="Stretch">
                                <TextBlock x:Name="alertsButtonCount"
                                           Style="{ThemeResource Icon}"
                                           FontFamily="Segoe UI Symbol"
                                           FontSize="32"
                                           Foreground="#FF4A90E2"
                                           TextAlignment="Center"
                                           Text="4"
                                           Margin="0,0,0,4" />
                                <TextBlock x:Name="alertsButtonMessage"
                                           FontSize="16"
                                           Foreground="#FF4A90E2"
                                           Text="Show Alerts"
                                           TextAlignment="Center"
                                           LineHeight="20"
                                           TextWrapping="WrapWholeWords" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <!-- Content Frame -->
                <Frame x:Name="contentFrame"
                       Grid.Row="2"
                       Grid.ColumnSpan="2" />

                <!-- Notification Panel -->
                <SplitView x:Name="notifications"
                           DisplayMode="Overlay"
                           OpenPaneLength="330"
                           PaneBackground="#CC515C6B"
                           Grid.Column="2"
                           Grid.Row="1"
                           PanePlacement="Right"
                           PaneClosing="notificationsPaneClosing">
                    <SplitView.Pane>
                        <ListView />
                    </SplitView.Pane>
                </SplitView>

            </Grid>
        </SplitView.Content>
    </SplitView>
</Page>
