<Page
    x:Class="Agronomist.Views.GraphingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Agronomist.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Grid Background="DimGray">
        <StackPanel >
            <Grid>
                <TextBlock Text="Select a Crop" Style="{StaticResource Subtitle}" Height="50" Width="124" HorizontalAlignment="Left" Margin="2,2,0,2" TextAlignment="Center"/>
                <ComboBox ItemsSource="{x:Bind ViewModel.CropRunList, Mode=OneWay}" Height="50" Width="350" SelectionChanged="CropCycleSelected" Margin="131,2,0,0">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock x:Name="SensorToggleButton"
                                       Text="{ Binding Value}"
                                       Margin="2,2,2,2" >
                            </TextBlock>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>

        </StackPanel>
    </Grid>
</Page>