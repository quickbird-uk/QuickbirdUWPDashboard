<Page x:Class="Agronomist.Views.LandingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid Background="#FFEBEBEB">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="216" />
        </Grid.RowDefinitions>
        
        <!-- First Row -->

        <Rectangle x:Name="HeroBackground"
                   Fill="#FF3498DB">
        </Rectangle>
        <Image x:Name="Quickbird_Logo"
               Source="ms-appx:///Assets/logo-qb-light-medium.png"
               Height="60"
               Width="210"
               Margin="0,56,0,0"
               HorizontalAlignment="Center"
               VerticalAlignment="Top">
        </Image>

        <!-- Second Row -->

        <Button Grid.Row="1"
                Foreground="DimGray"
                Background="#00000000"
                BorderBrush="DimGray"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Width="340"
                Height="60"
                Padding="0"
                Margin="0,56,0,12"
                Click="Authenticate"
                BorderThickness="1">
            <Button.Transitions>
                <TransitionCollection>
                    <EdgeUIThemeTransition Edge="Bottom" />
                </TransitionCollection>
            </Button.Transitions>
            <Button.Content>
                <StackPanel Orientation="Horizontal">
                    <Image x:Name="Microsoft_Logo"
                           Source="ms-appx:///Assets/logo-msft.png"
                           Height="30"
                           Width="30"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Top"
                           Stretch="UniformToFill"
                           Margin="0,0,16,0" />
                    <TextBlock Text="Sign in using Microsoft Account"
                               FontFamily="Segoe UI"
                               FontWeight="Normal"
                               FontSize="18"
                               TextAlignment="Left"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Center"
                               Margin="0,0,0,2" />
                </StackPanel>
            </Button.Content>
        </Button>
        <TextBlock Grid.Row="1"
                   Foreground="DimGray"
                   FontFamily="Segoe UI"
                   FontWeight="Normal"
                   FontSize="12"
                   TextAlignment="Center"
                   TextWrapping="Wrap"
                   Width="340"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   Margin="0,0,0,56">
            <TextBlock.Transitions>
                <TransitionCollection>
                    <EdgeUIThemeTransition Edge="Bottom" />
                </TransitionCollection>
            </TextBlock.Transitions>
            Your Microsoft Account is the account you use to download 
apps from the Windows Store.
        </TextBlock>
    </Grid>
</Page>